spring:
  datasource:
    url: jdbc:tc:postgresql:16:///approval
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
